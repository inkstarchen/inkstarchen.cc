## 代理

反向代理（reverse proxy）正向代理(forward proxy)(proxy)

1. 直接访问：没有中间商，直接向目标服务器发送请求，得到结果。
2. 正向代理：通过代理服务器，代发请求，最后经过代理服务器接收到结果
	1. 代理服务器需要自己手动配置，告诉浏览器从代理服务器在何处
3. 反向代理：为了解决负载的问题，请求者不知道自己被代理，而是在服务器端存在一个中间代理，用于分配请求，实现负载均衡。