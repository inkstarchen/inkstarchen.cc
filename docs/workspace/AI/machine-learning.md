## 机器学习

**机器学习的研究目标** ：利用经验改善系统自身的性能

**训练的根本目标：** 获得泛化能力（应对未来未见测试样本的能力）



### 基本术语

- 输出空间 == 标记空间
- 机器学习的任务分类方法
- **基本假设**：独立同分布
- **名词解释：**
	- 假设空间：所有可能性的集合
	- 版本空间：所有与样本符合的假设的集合
	- 归纳偏好

## 模型评估与选择

- 训练误差与泛化误差、测试误差的关系
- 过拟合、欠拟合

- 训练误差分类和问题分类

### 评估方法

> 每个方法计算错误率

- **留出法（hold-out）**： 分出两个集合

- **交叉验证法（cross-validation）**：k划分，取一作为测试
    - 特例：留一法

- **自助法（bootstrapping）**: 随机采样m个

- 验证集和测试集

### 性能度量（performance measure）

- 各类任务的常用度量方法

- 错误率，精度、查全率、查准率、精确度、准确度、召回率、
- P-R 曲线和平衡点
- F1-score计算 和 $\beta$变体 、宏变体 和 微变体

| 真实情况\预测结果| 正例 | 反例 |
|--- |---|---|
|正例|TP（真正例） | FN（假反例）|
|反例|FP（假正例） | TN（真正例）|

- 真正例率和假正例率：各自有多少预测对
**ROC曲线**的定义和构建 和 AUC值

**AUC值**：ROC曲线下的面积 | 衡量了样本预测的排序质量

- 需要每个样本有值预测，并正反例，逐个设置阈值


**代价敏感错误率**：非均等的代价求和平均

- 为错误赋予**非均等代价**
- 代价矩阵:$cost_{ij}$表示将第$i$类预测为第$j$类的代价.
- 二分类任务为例

|真实\预测| 第 0 类 | 第 1 类|
|---|---|---|
|第0类| 0|$cost_{01}$ |
|第1类|$cost_{10}$ | 0 |

- 非均等代价下，ROC曲线无法直接反映出学习器的期望总体代价，但是“代价曲线（cost curve）”可以.
- 横轴为正例概率代价$P(+)cost = \frac{p \times cost_{01} }{p \times cost_{01} + (1-p) \times cost_{10} }$, 其中$p$是样例为正例的概率
- 纵轴为归一化代价$cost_{norm} = \frac{FNR \times p \ times cost_{01} + FPR \times (1-p) \times cost_{10} }{p \times cost_{01} + (1-p) \times cost_{10} }$ 其中$FNR = 1- TPR$是假反例率
- ROC曲线上每个点，都对应代价平面上一条线段，取所有线段的下界，围成的面积为在所有条件下学习器的期望总体代价

#### 比较检验： 估计泛化性能

> 性能比较：测试性能不等于泛化性能，测试性能随测试集变化，机器学习算法本身具有随机性

统计假设检验（hypothesis test）为学习器性能比较提供了重要依据：

- 基于假设检验结果，我们可以推断，若在测试集上观察到学习器A比B好，则A的泛化性能是否在统计意义上优于B，以及这个结论的把握有多大。

**假设检验**：

泛化错误率为$\epsilon$的学习器被测得测试错误率为$\hat{\epsilon}$的概率：

$$P(\hat{\epsilon};\epsilon) = \binom{m}{\hat{\epsilon} \times m} \epsilon^{\hat{\epsilon} \times m}(1-\epsilon)^{m-\hat{\epsilon}\times m}$$

- 对$\epsilon$求偏导可知，当$\epsilon = \hat{\epsilon}$时，概率最大，符合二项分布

**二项检验**：置信度

- 简单解释：在假设为真的前提下的到观测结果的概率与我们愿意冒的风险做比较，来确定假设的置信度 

- 考虑假设$\epsilon \leq \epsilon_0$,则在$1-\alpha$的概率内所用观测到的最大错误率如下式计算：

$$\bar{\epsilon} = max \epsilon \quad s.t. \sum^{m}_{i=\epsilon_0 \times m + 1} \binom{m}{i}(1-\epsilon)^{m-i} < \alpha$$

- 假若测试错误率$\hat{\epsilon}$小于临界值$\bar{\epsilon}$，则根据二项检验可以得出结论：在$\alpha$的显著度下，假设$\epsilon \leq \epsilon_0$不能被拒绝.


- 很多时候我们会做多次训练和训练，得到多个测试错误率，此时可以使用**t-检验(t-test)**（P40）
- 进一步有**交叉验证t-检验**（P41）、**McNemar检验**、**Firedman检验**与**Nemenyi后续检验**


#### 偏差与方差 | 为什么具有这样性能

假设噪声期望为0，期望泛化误差拆解为偏差、方差与噪声之和

- 偏差-方差窘境（bias-variance dilemma）

### 线性回归分析

- 最小二乘法估计：使用均方误差最小化
- 通过梯度下降法求解
- 对数线性回归：线性到指数上
- 联系函数
- 对数几率函数与对数几率回归

- 批量梯度下降(Batch Gradient Descent, BGD)
- 随机梯度下降(Stochastic Gradient Descent,SGD)
- 小批量梯度下降(Mini-batch Gradient Descent, MBGD)

## 有监督降维

### 线性判别分析(Linear Discriminant Analysis, LDA)

> 给定已分类好的数据，希望找到区分这些类别的特征. 降维只是副产物，既然
> 有了区分它们的特征，就能够决定保留哪些主要特征。

**使用概念**:
- 类内散度矩阵、类间散度矩阵、广义瑞利商
- 求解使用奇异值分解

**局限性：**

- 默认每个类别的数据都遵循正态分布，且数据分布形状差不多.
- 对小样本，高维数据脆弱
- 矩阵信息来源于 $C$ 个类别中心，因此最多只能捕捉$C-1$个差异方向.
- 只能发现线性差异。



### 多分类学习

- 拆分策略：OvO、OvR、MvM | 针对类别而言的
- ECOC纠错输出码与最优编码 | 多分类器编码 取相近值
- 类别不平衡的过采样和欠采样

### 类别不平衡

- 结果再缩放
- 过采样、欠采样、结果缩放


## 决策树 CART（Classification And Regression Tree）

**解决什么问题**：基于哪些特征，以及如何根据这些特征的取值，来对目标变量做出一系列判断

**本质概括：** 决策树是**根据样本数据的分布情况**，使用信息熵或是纯度度量来确定分支条件和样本集划分，**最终依据最终样本集中的数据真值情况**做出预测，分类树是真值的类别众数，回归树则是平均值

> 关键点：1. 决策树是数据驱动的，树的结构完全取决于数据分布。 2. 预测值完全来源于训练样本，因此决策树不能外推

**核心逻辑链：**

- **输入**：带有标签的训练样本集（分类树标签是类别，回归树标签是数值）。
- **构建过程（训练）**：
  - 在每个节点，根据样本数据的分布情况（分类树看类别比例，回归树看数值离散程度）。
  - 使用纯度度量指标（分类树用信息熵/基尼系数，回归树用均方误差MSE）。
  - 选择能最大程度提升纯度的特征和分裂阈值，确定分支条件。
  - 递归划分样本集，直到满足停止条件。
- **叶子节点存储**：
  - 不再划分的叶子节点，依据该节点内样本的真实标签做出预测准备。
  - 分类树：存储类别众数（出现最多的那个类别）。
  - 回归树：存储数值平均值。
- **预测（推理）**：新样本顺着树走到叶子节点，直接输出该节点预先存好的众数或平均值。

**与聚类的异同**

| 模型 | 驱动力 | 目标 | 总结 |
| --- | --- | --- | --- |
| 决策树 | 历史决策（标签） | 做出与历史一致的决策 | 以历史决策为主导，做出与历史相近的决策|
| 聚类 | 特征相似性 | 找出自然形成的相似类别 | 以特征为主导，找出相似的类型 | 


- **信息熵（entropy）**和**信息增益**（information gain）以及**基尼指数**

### 剪枝处理：
- 通过性能验证判断

- 预剪枝：在生成过程中，进行剪枝
- 后剪枝：先生成完，向上考虑
	
连续属性离散化（二分法） 

- 通过信息增益率来确定划分点

缺失值处理

- 分概率进入后续结点

多变量决策树

- 在一个划分规则内使用多个属性，多次分段划分

## 神经网络

- 加权传递 + 阈值 + 激活函数
- 无法解决异或问题
- 误差逆传播公式

### 防止进入局部最小的手段
- 模拟退火
- 随机梯度下降
- 其它神经网络 

## 贝叶斯分类器

### 贝叶斯决策论

- 前提假设条件
- 条件风险计算
- 总体风险计算

- **贝叶斯判定准则(Bayes decision rule)** | 关于如何选择判定的函数

- **贝叶斯最优分类器(Bayes optimal classifier)** 和 **贝叶斯风险(Bayes risk)**
- 目标为最小化分类错误率的实例

- **判别式(discrimnative)模型** 和 **生成式(generative)模型**
- 什么是似然(likelihood)和证据(evidence)因子
- 先验概率怎么得到
- 怎么考虑连续型的属性值


### 极大似然估计(Maximum Likelihood Estimation, MLE)

- 估计类条件概率的常用策略
- 极大似然估计的朴素思想
- 对数似然(log-likelihood)的引进
- 极大似然估计的局限性

### 朴素贝叶斯分类器(naive Bayes classifier)

- **属性条件独立性假设（attribute conditional independence assumption）**
- 先验概率 和 属性条件概率 在样本集上的估计
- 如何使用朴素贝叶斯计算分类结果
- 缺失特定样本 | 拉普拉斯修正(Laplacian correction)做法
- 懒惰学习(lazy learning)

### 半朴素贝叶斯分类器

- 常用策略：独依赖估计(One-Dependent Estimator)|问题转化
- SPODE(Super-Parent ODE)
- TAN(Tree Augmented naive Bayes) | 条件互信息
- AODE(Averaged One-Dependent Estimator)

### 贝叶斯网(Bayesian network)


> 也称为 **信念网**（brief network）

- 有向无环图(Directed Acyclic Graph, DAG) 和 条件概率表(Conditional Probability Table, CPT)
- 属性的联合概率分布计算
- 三个典型依赖关系：同父结构、V型结构、顺序结构
- 边际独立性
- “有向分离”(D-separation)  和 "道德图" (moral graph)

#### 学习

- 首要任务 与 常用方法 | 评分搜索
- 最小描述长度(Minimum Description Length, MDL)准则
- (Akaike Information Criterion, AIC) 和 (Bayesian Information Criterion, BIC)

#### 推断

- 近似推断 | 吉布斯采样(Gibbs sampling)

### EM算法

- 存在 未观测变量 “隐变量”(latent variable) 的估计
- E 步(Expectation)：估计隐变量
- M 步(Maximization)：估计参数

## 集成学习(Ensemble Learning)

- 基学习器 | 基学习算法 | 同质 和 异质
- 对个体学习器的要求 | 霍夫丁不等式
- 串行生成的**序列化方法** 和 同时生成的**并行化方法**

### Boosting

- AdaBoost 的加性模型 | 指数损失函数 与 替代损失函数
- 偏差-方差视角

### Bagging 与 随机森林
- Booststrap AGGregatING | 自主采样 | 包外估计
- 预测输出结合
- 偏差-方差视角
- 随机森林的思想

### 结合策略

- 平均法 和 加权平均法
- 投票法： 绝对多数投票法(majority voting) | 相对多数投票法(plurality voting) | 加权投票法(weighted voting)
- 硬投票 和 软投票
- 学习法 Stacking | 次级学习器 | 使用未使用样本 | 多响应线性回归(Multi-response Linear Regression, MRLR)

### 多样性

- 误差-分歧分解：适用于回归学习
- 多样性度量(diversity measure) | 预测结果列联表
- 不合度量 | 相关系数 | Q-统计量 | $\ker$-统计量
- 数据样本扰动 与 稳定基学习器
- 输入属性扰动 | 输出表示扰动 | 算法参数扰动

## 支持向量机

### 前置

- 拉格朗日，KKT条件

### 自查清单

- 什么是支持向量（support vector）、间隔（margin）、最大间隔
- 计算：经验化选取，然后检查约束，确定参数
- 为啥要用到核函数，它的定义和要求
	- 高维乘积计算的模拟
	- 一些常用的核函数
- SMO（Sequential Minimal Optimization）的基本思路
- 软间隔和硬间隔的区别、啥是松弛变量
- 替代损失函数
- 结构风险和经验风险
- 什么是支持向量回归的思想
- 什么是核线性判别分析

## 分类任务

### 聚类

> 以自定义的特征和距离度量标准，去发现样本中的类别关系.

**关键概念**

- 距离度量 和 相似度度量
- 外部指标 和 内部指标


#### 原型聚类

- k 均值散发： k个类别算最小平分误差
- 学习向量量化： 学习原型向量
- 高斯混合聚类：使用概率模型
- 密度聚类：密度直达、密度可达、密度相连
- 层次聚类：不断合并，最近的

## 降维与度量学习

- k近邻学习(k-Nearest Neighbor, kNN)


### 低维嵌入

**基于假设**：与学习任务密切相关的可能仅是某个低维分布，即高维空间中的一个低维“嵌入”

#### “多维缩放”（Multiple Dimensional Scaling, MDS）

- 要求：原始空间中样本之间的距离在低维空间中得以保持
- 过程：需要中心化，由不变的距离矩阵求取降维后的内积矩阵$\textbf{B}$,对内积矩阵做特征值分解
- 目标：求得每个样本的低维坐标

> 拓展到普遍的线性变换的**线性降维方法**

### 主成分分析（Principal Component Analysis, PCA）

> PCA不关心后续任务，唯一目标时，找到一个低维表示，并尽可能地保留原始数据中的信息，即包含信息量最大，使样本呈现最大差异的维度.

**看待问题的两面**

- **最近重构性**：样本点到这个超平面的距离都足够近
- **最大可分性**：样本点在这个超平面上的投影能尽可能分开（方差尽可能大）

> 其等价性来源于数据的空间总方差守恒，在一个维度上方差越大，在其正交维度上方差越小.

**过程**

- 中心化
- 计算样本的协方差矩阵
- 特征值分解
- 选取特征向量

**局限性**

- 只能发现数据中的线性结构，无法处理非线性关系.
- 对尺度敏感（需要标准化）
- 无法处理缺失值
- 对异常值敏感
- 可解释性下降(因为保留的特征变为了原始特征的线性组合)
- 无法处理类别特征
- 启发式特征选择

### 核化线性降维

> 极度依赖自动化函数和参数探索，特定性使用，不适用于泛化性问题。

- 将“原本采样的”低维空间称为 “本真”（intrinsic）低维空间

**核主成分分析**（Kernelized PCA, KPCA）

通过核函数，利用高维信息进行降维

> 它的计算开销较大。

### 流形学习（manifold laerning）

- 着眼于局部性质

> "流形" 是在局部与欧氏空间同胚的空间.

**等度量映射（Isometric Mapping， Isomap）**

- “测地线”（geodesic）距离，沿曲面的最近距离

- 计算近邻连接图上的最短距离,可以采用Dijkstra算法或Floyd算法.得到任意两点的而距离后，可以通过MDS方法来获得坐标.

> 想要进一步得到映射，目前只能通过训练一个回归学习器来进行预测.

- 近邻图的构建(均有不足):数目阈值、距离阈值

**局部线性嵌入(Locally Linear Embedding, LLE)**

- 试图保持邻域内样本之间的线性关系

对$\mathbf{M} = (\mathbf{I} - \mathbf{W})^{\mathbf{T}} (\mathbf{I} - \mathbf{W})$进行特征值分解，取特征向量得到矩阵$\mathbf{Z}^{\mathbf{T}}$

### 度量学习(metric learning)

> 直接学习一个合适的距离度量

马氏距离(Mahalanobis distance)：对每个维度的距离施加不同权重

可对近邻分类器做距离概率的拓展

即求解$\underset{\mathbf{P}}{min} \; 1 - \overset{m}{\underset{i=1}{\sum}}\underset{j \in \Omega_i}{\sum} \frac{exp(-\|\mathbf{P}^{\mathbf{T}}x_i - \mathbf{P}^{\mathbf{T}}x_j\|^2_2)}{\sum_l exp(-\|\mathbf{P}^{\mathbf{T}}x_i - \mathbf{P}^{\mathbf{T}}x_l\|^2_2)}$


## 特征选择与稀疏学习

> 目的： 选择相关特征，去除无关特征和冗余特征（可被其它特征计算得出）


**子集搜索**：贪心或穷举
**子集评价**：以子集A划分数据集的信息熵增益




### **特征选择方法分类**


#### 过滤式选择（filter）

先对数据集进行特征选择，再训练学习器，特征选择过程与后续学习器无关.

**Relief(Relevant Features)**方法

设计了一个“相关统计量”来度量特征的重要性

- 统计量是一个分量，每个分量对应于一个初始特征。
- 特征子集的重要性由子集中每个特征所对应的特征统计量分量之和决定.

定义 **猜中近邻** 与 **猜错近邻**， 相关统计量对应于属性 $j$ 的分量为：

$$\delta^j = \underset{i}{\sum}-diff(x_i^j, x_{i,nh}^j)^2 + diff(x_i^j,x_{inm}^j)^2,$$

> Relif 是为二分类问题设计的，拓展变体 Relief-F能处理多分类问题.

#### 包裹式选择

将最终要使用的学习器的性能作为特征子集的评价准则.

LVW (Las Vegas Wrapper)方法，在拉斯维加斯方法(Las Vegas method)框架下使用随机测量进行子集搜索，再使用交叉验证法估计学习器误差.

> 由于随即策略，和每次都要训练学习器的原因，这个方法的计算开销很大. 在有运行时间限制的情况下，可能给不出解.

#### 嵌入式选择

> 将特征选择放在训练过程中完成.

**岭回归(ridge regression)**:引入惩罚项$\lambda \|w\|^2_2$，防止过大的系数.

若采用L1范数，则称其为LASSO(Least Absolute Shrinkage and Selection Operator)

### 压缩感知

## 半监督学习

- 纯半监督学习：开放世界假设
- 直推学习：封闭世界假设

### 生成式方法

- 假设数据分布，通过EM算法来估计参数

### 半监督SVM

TSVM： 先生成超平面，进行标记指派，交换两个异类可能错误，不断迭代，增加权重。

### 图半监督学习

- 将数据集映射为图，然后进行关联性染色

### 基于分歧的方法

多个训练器，逐步排除，共同进步

### 半监督聚类

## 概率图模型

> 用图来表达变量相关关系的概率模型

- 有向无环图：贝叶斯网
- 无向图：马尔可夫网

### 隐马尔可夫模型(Hidden Markov Model, HMM)

- 结构信息
- 状态转移概率
- 输出观测概率
- 初始状态概率

#### 三个基本问题

- 给定模型、产生观测序列的概率
- 给定模型核观测序列：最匹配的状态序列
- 给定观测序列，如何跳转模型参数使其出现概率最大

### 马尔可夫随机场(Markov Random Field, MRF)
- 团和极大团
- 通过团计算观测变量出现的联合概率

> 全局马尔可夫性(global markov property): 给定两个变量子集的分离集，则这两个变量子集条件独立

> 局部马尔科夫性（local Markov property）:给定某变量的邻接变量，则该变量条件独立于其它变量

> 成对马尔可夫性（pairwise Markov property）:给定所有其它变量，两个邻接变量条件独立

- 势函数：表示两个变量之间的相关关系

### 条件随机场(Conditional Random Field, CRF)

> 处理条件概率，而马尔可夫随机场处理联合概率

- 对多个变量在给定观测值后的条件概率进行建模

**链式条件随机场**(chain-structured CRF)

### 学习与推断

#### 精确推断

**变量消去**

**信念传播**：将变量消去中的求和操作看作一个消息传递过程

#### 近似推断
- 采样：使用随机化方法完成近似
- 使用确定性近似完成近似推断：变分推断

**MCMC采样**（Markov Chain Monte Carlo, MCMC）

- Metropolis-Hastings(MH)算法：基于拒绝采样来逼近平稳分布
- 吉布斯采样:MH算法特例

**变分推断**：使用已知简单分布来逼近需要推断的复杂分布.

### 话题模型

- 生成式有向图模型，主要处理离散型的数据

## 规则学习

- 使用多条规则 | 冲突消解 
-  优先级规则

### 序贯覆盖

- 逐条归纳，去除规则覆盖的训练样例，分治策略。
- 组合爆炸
- 两种做法：特化（泛化性强）和泛化（少样本）
- 特化贪心-> 局部最优
- 集束搜索，保留b个最优待下轮选用

### 剪枝优化

### 一阶规则学习

FOIL增益

最小一般泛化

逆归结

## 强化学习

- 强化学习的过程抽象和空间
- 强化学习的目标
- 怎么衡量学习结果的好坏和方法
- K-摇臂赌博机的设定
- 探索和利用的平衡算法

### 有模型学习

- 定义和目的
- 策略评估的函数定义
- Bellman等式的推导
- 策略怎么改进
- 策略迭代和值迭代过程

### 免模型学习

- 蒙特卡洛强化学习
	- 同策略和异策略区别
- 时序差分学习
- Q-学习

### 值函数近似

- 想法和优化公式

## 卷积神经网络

- 产生思路和适用


## 自注意力机制

- Q K V的意义
- 基本想法

## 序列到序列

- 结构树
- transformer.

### LLM

- 拒绝向量
- sparse coding
- Knowledge Distillation
- 加语音微调
- 深度思考：Verification Explore Planning