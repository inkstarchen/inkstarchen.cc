
### 间断点

> 条目质量：中 | 定义完全，但无例子

**可去间断点：** 左右极限存在且相等，但不等于$f(x_0)$或者$f(x)$在x_0处无定义.

**不可去间断点：** 左右极限存在且不相等（跳跃间断点）,或者左右极限至少有一个不存在.

### 定积分

> 条目质量：中 | 定义完全，但是仍然没有具体说明，例如分化取点，黎曼和没有链接指向，回看时难以回忆。

* 定义：
	* 有限，有界
	* 黎曼和
		* 性质：可积的结果与分化取点无关
* 性质
	* $$线性性质：{\int}_a^b[k_1f(x)+k_2g(x)]dx=k_1{\int}_a^bf(x)dx+k_2{\int}_a^bg(x)dx$$
	* 乘积可积性
	* $$保序性:f(x){\geq}g(x)=>{\int}_a^bf(x)dx{\geq}{\int}_a^bg(x)dx$$
	* 绝对可积性
	* 设$f(x)在[a,b]$上可积，则$|f(x)|在[a,b]$上也可积，且成立$$|\int^b_af(x)dx|\leq\int_a^b|f(x)|dx.$$

### 反常积分

> 条目质量：低 | 没有例子，且条目性质不明，先放在定义中

* 无限区间
	* 定积分加极限（不存在则称其发散）
	* 随机插入一个点（负无穷到正无穷）（需要均收敛）变为b时候应当用两个不同的b
	* 无限区间的面积问题




## 技巧

### 变上限的导数

> 条目质量：低 | 缺少例子，只有思想且解释不足

将上限的变量换元，成基础情况。

**例子：** 待补充

### 定积分计算方法

> 条目质量：低 | 方法不多但是重要的2没有解释，无法马上理解

1、算出不定积分代入
2、进行分布定积分计算（利用零处以及相似）

### 不定积分积分方法

> 条目质量：中 | 缺少例子，虽然有丰富的类型，但是并不能直接对应到具体的例子。

1. 变形+直接积分法
2. 第一类换元积分，凑微分法。（换元后可以直接积分）（遇见三角，积化和差，和差化积）
* 巧妙根式换元$$x+t=\sqrt{x^2-x+1}$$

1. 第二类换元，利用中间量积分。（三角换元、倒数变换、根号中提出凑微分。）
2. 分部积分（用于降低复杂度、转换成多项式。）（多次分部求出相同形式，混合求解）（在分式情况下能够减低分母x的次数，对分子多次求导）
3. n次方的不定积分，分部降幂，找递推。
$$
\sqrt\frac{x-a}{x+a}使用acosu换元
$$
$$
\sqrt{a^2-u^2}使用分部积分方法
$$
$$
x\sqrt{\frac{x}{2a-x}}=>\frac{x^2}{\sqrt{2ax-x^2}}
$$

先利用分部将ln转换成多项式。
$$
\frac{cosx}{sinx+cosx}寻找配套积分\frac{sinx}{sinx+cosx}
$$
6. 寻找配套积分
$$e^xcosx,e^xsinx找欧拉公式$$

### 收敛发散的判断

> 条目质量： 中 | 提供思路，但无例子

进行中间的放缩判断


### 泰勒公式

> 条目质量： 中 | 查阅够用，但是没证明，没使用的例子

#### 带Peano余项的Taylor公式
设$f(x)在x_0$处有n阶导数，则存在$x_0$的一个邻域，对于该邻域中的任一点$x$，成立$$f(x)=f(x_0)+f'(x_0)(x-x_0)+\frac{f''(x_0)}{2!}(x-x_0)^2+...+\frac{f^{(n)}(x_0)}{n!}(x-x_0)^n+r_n(x),$$
其中余项$r_n(x)$满足$$r_n(x)=o((x-x_0)^n).$$
#### 带Lagrange余项的Taylor公式
设$f(x)在[a,b]$上具有n阶连续导数，且在$(a,b)$上有n+1阶导数.设$x_0\in [a,b]$为一定点，则对于任意$x\in [a,b]$，成立$$f(x)=f(x_0)+f'(x_0)(x-x_0)+\frac{f''(x_0)}{2!}(x-x_0)^2+...+\frac{f^{(n)}(x_0)}{n!}(x-x_0)^n+r_n(x),$$
其中余项$r_n(x)$满足$$r_n(x)=\frac{f^{(x+1)}(\xi)}{(n+1)!}(x-x_0)^{n+1},\quad \xi在x和x_0之间.$$
### 有理函数的不定积分及其应用

> 条目质量： 低 | 缺乏上下文，完全不知所云

分式的积分计算，有三角的可以拥有万能公式（迫不得已）
统一化成分式积分，然后分解。但这样过于麻烦。
$$
\frac{p(β-iγ)}{q^*(β-iγ)}=μ(β-iγ)+v
$$
其中可以把它化成右侧形式，是为了统一形式。