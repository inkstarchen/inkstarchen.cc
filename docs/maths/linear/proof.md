# 线性代数证明

> 条目质量： 低 | 层次划分不清 | 且无例子

任意性证明：有时候我们会遇到这样的证明问题，要求我们证明对于任意的满足某一条件的元素，都有某一命题成立. 这一问题十分平凡，我们只需 “任取” 一个满足条件的元素，然后证明它满足命题即可，其关键就在于我们选取元素是任意的. 例如我们证明整数集合关于加法运算构成群，我们验证运算封闭性、逆元存在性都是任意性

证明.
析取式证明：即证明若 p 成立，则 q 或 r 二者之一成立. 析取的证明比合取原理复杂，事实上一般而言我们的证明思路就是：假设 q 不成立，证明 r（即证明 ¬q → r），或者假设 r 不成立，证明 q（即证明 ¬r → q），这样我们就证明了 q 或 r 二者必有

其一成立，因为它们不会同时不成立. 最

1. 充要性证明可以用反证法（基本思路是假设反面成立，推出矛盾，使得正面成立）
2. 线性无关，维数相同，扩张即得到
3. 唯一性证明：假设有两个成立，证明这两个相同。
4. 先按照题意的思路去将条件用数学语言表示，再构造题目条件的形式，必要时候可以全部化出试试
转换成矩阵去思考
用列向量乘矩阵去得到每行元素相加的信息
思考的维度很重要，先从高维度思考
n层次的证明用递推的形式

与秩相关的证明可以用变换分块矩阵来做
做任何题目，先拆解表示出来
如果A满秩，则其必然能够化成一个单位向量和零向量的组合

AB=0的情况，则视作AX=0的解空间的讨论情况。（从不同的眼光去看待同样的式子）
代数余子式是行列式计算

## 上确界证明

- 放缩出上界
- 证明上界可取到

**例题**：Operator norms

> 从绝对数不等式入手证明其界，往题目的形式靠拢,凑形式是数学题目的重要手段.

Suppose that $A \in \mathbb{R}^{m \times n}$, which can be viewed as a linear transformation from $\mathbb{R}^n$ to $\mathbb{R}^m$. Please show that the following operator norms’ equality.

a.Let

$$
\|A\|_1 = \sup_{x \in \mathbb{R}^n,\, x \neq 0} \frac{\|Ax\|_1}{\|x\|_1}.
$$

Please show that

$$
\|A\|_1 = \max_{1 \le j \le n} \sum_{i=1}^{m} |a_{ij}|.
\tag{3}
$$

b.Let

$$
\|A\|_\infty = \sup_{x \in \mathbb{R}^n,\, x \neq 0} \frac{\|Ax\|_\infty}{\|x\|_\infty}.
$$

Please show that

$$
\|A\|_\infty = \max_{1 \le i \le m} \sum_{j=1}^{n} |a_{ij}|.
\tag{4}
$$
